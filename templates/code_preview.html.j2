<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>{{ name }} Theme Preview</title>
  <style>
    :root {
      --bg: {{ colors.editor.bg }};
      --surface: {{ colors.editor.surface }};
      --fg: {{ colors.syntax.foreground }};
      --comment: {{ colors.syntax.comment }};
      --keyword: {{ colors.syntax.keyword }};
      --function: {{ colors.syntax.function }};
      --string: {{ colors.syntax.string }};
      --type: {{ colors.syntax.type }};
      --constant: {{ colors.syntax.constant }};
      --cursor: {{ colors.other.cursor }};
      --selection: {{ colors.other.selection }};
    }

    * {
      box-sizing: border-box;
    }

    html, body {
      background-color: transparent;
      color: var(--fg);
      font-family: 'CaskaydiaCoveNerdFont', monospace;
      margin: 0;
      padding: min(2vw, 2vh);
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      width: 100vw;
    }

    .terminal {
      background-color: var(--bg);
      padding: min(3vw, 3vh);
      border-radius: min(2vw, 2vh);
      box-shadow: 0 10px 25px rgba(0, 0, 0, 0.4);
      position: relative;
      width: 90vw;
      height 45vh;
    }

   .terminal::before {
      content: '';
      position: absolute;
      top: min(3vw, 3vh);
      right: min(3vw, 3vh);
      width: 1.5vw;
      height: 1.5vw;
      background-color: #ff5f56;
      border-radius: 50%;
      box-shadow:
        -2vw 0 0 #ffbd2e,
        -4vw 0 0 #27c93f;
    }

    pre {
      margin: 0;
      font-size: 1.8vw;
      line-height: 1.3;
      white-space: pre;
    }

    code {
      display: block;
      background-color: transparent;
    }

    /* Syntax highlighting */
    .comment    { color: var(--comment); }
    .keyword    { color: var(--keyword); }
    .function   { color: var(--function); }
    .string     { color: var(--string); }
    .type       { color: var(--type); }
    .constant   { color: var(--constant); }
  </style>
</head>
<body>
  <div class="terminal">
    <pre><code><span class="keyword">func</span> <span class="function">main</span>() {
  <span class="keyword">var</span> cache <span class="type">string</span>
  <span class="keyword">var</span> center <span class="type">bool</span>
  <span class="keyword">var</span> fallback <span class="type">string</span>
  <span class="keyword">var</span> protocol <span class="type">string</span>
  <span class="keyword">var</span> scale <span class="type">string</span>

  <span class="comment">// getting flags</span>
  flag.<span class="function">StringVar</span>(&protocol, <span class="string">"p"</span>, <span class="string">"auto"</span>, <span class="string">"Force protocol: kitty, iterm, sixel"</span>)
  flag.<span class="function">BoolVar</span>(&center, <span class="string">"center"</span>, <span class="constant">true</span>, <span class="string">"rather or not to center align the image"</span>)
  flag.<span class="function">StringVar</span>(&fallback, <span class="string">"f"</span>, <span class="string">"ascii"</span>, <span class="string">"fallback protocol"</span>)
  flag.<span class="function">StringVar</span>(&scale, <span class="string">"scale"</span>, <span class="string">"1x1"</span>, <span class="string">"float for scaling the image"</span>)
  flag.<span class="function">BoolVar</span>(&cache, <span class="string">"cache"</span>, <span class="constant">true</span>, <span class="string">"rather or not to cache the heavy operations"</span>)
  flag.<span class="function">BoolFunc</span>(<span class="string">"version"</span>, <span class="string">"prints the version number"</span>, <span class="keyword">func</span>(s <span class="type">string</span>) <span class="type">error</span> {
      <span class="function">println</span>(version)
      <span class="keyword">defer</span> os.<span class="function">Exit</span>(<span class="constant">0</span>)
      <span class="keyword">return</span> <span class="constant">nil</span>
    })
}
</code></pre>
  </div>
</body>
</html>
